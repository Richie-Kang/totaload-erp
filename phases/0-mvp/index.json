{
  "project": "중고차 수출 ERP (Totaload ERP)",
  "phase": "0-mvp",
  "steps": [
    {
      "step": 0,
      "name": "project-setup",
      "status": "completed",
      "started_at": "2026-05-13T08:53:00+0900",
      "summary": "npm workspaces monorepo: frontend/ (Vite+React+TS+Tailwind, eslint flat config, vitest dummy), backend/ (Express+TS, src/app.ts+index.ts with GET /api/health, tsx dev, vitest+supertest, migrate stub), ocr-service/ (FastAPI app/main.py GET /health, app/__init__.py, tests/test_smoke.py, requirements.txt incl pypdf/pypdfium2/reportlab/httpx). Root package.json scripts: lint/build/test (test runs workspace tests + `python3 -m pytest` in ocr-service). .gitignore extended (dist, .env*, storage/, __pycache__). .env.example with DATABASE_URL/BACKEND_PORT/OCR_SERVICE_URL/OCR_PORT/CORS_ORIGIN/STORAGE_DIR/TEMPLATE_PATH/CODEX_AUTH_JSON. assets/malso_application_template.pdf (copied+renamed from root PDF; 12 AcroForm fields verified incl trailing-space 'vehicle_year '), assets/samples/*.jpg. storage/.gitkeep. docs/ filled from master plan: PRD.md (§1), ARCHITECTURE.md (§2.1-2.8), ADR.md (ADR-001..010 + philosophy + 'confirmed codex CLI interface' note), UI_GUIDE.md (§4.1-4.7). Deploy skeleton (TODO-only): Dockerfile.frontend/backend/ocr, docker-compose.yml, render.yaml, README.md. codex CLI: INSTALLED (codex-cli 0.128.0); supports image input `-i/--image`, `-s/--sandbox read-only`, `--skip-git-repo-check`, `--output-last-message <FILE>`, `--json`, `--output-schema <FILE>` — step2 can proceed (image input supported). NOTE: machine has `python3` not `python`; root test script and README use `python3`.",
      "completed_at": "2026-05-13T09:05:10+0900"
    },
    {
      "step": 1,
      "name": "db-schema",
      "status": "completed",
      "started_at": "2026-05-13T09:05:10+0900",
      "summary": "PostgreSQL schema + pool + idempotent migration (pg, no ORM). backend/src/db/schema.sql: pgcrypto ext, vehicles + documents tables (columns/types exactly per ARCHITECTURE.md §2.4), indexes vehicles_reg_no_norm_idx / vehicles_vin_norm_idx (lower(replace(coalesce(...),' ','')) for partial search), vehicles_updated_at_idx (desc), documents_vehicle_id_idx, partial unique vehicles_vin_uniq (where vin is not null and vin<>''); no updated_at trigger (app sets updated_at=now()). backend/src/db/pool.ts: `pool` (pg.Pool from DATABASE_URL — no module-load crash if unset), `waitForDb(maxAttempts=10)` exponential backoff, `query<T>(sql, params?): Promise<pg.QueryResult<T>>`. backend/src/db/migrate.ts: `runMigrations(): Promise<void>` — waitForDb then runs schema.sql in one txn (idempotent, throws on failure); schema.sql resolved via import.meta.url. backend/src/db/migrate-cli.ts is `npm -w backend run migrate` target (build copies schema.sql to dist/db/). backend/src/index.ts now awaits runMigrations() before listen (exits 1 on failure). GET /api/health now returns {status:'ok', db:'ok'|'down'} via `select 1` ping (step3 adds ocr field). backend/test/migrate.test.ts (skips if no DATABASE_URL): runMigrations twice = no error, to_regclass('vehicles'/'documents') non-null, all 5 indexes present; health.test.ts updated for new shape. Deps added: pg, @types/pg. Verified against local postgres 16: migrate x2 idempotent, tables/indexes/columns correct, all tests pass; npm run build && npm run lint clean.",
      "completed_at": "2026-05-13T09:09:25+0900"
    },
    {
      "step": 2,
      "name": "ocr-service",
      "status": "completed",
      "started_at": "2026-05-13T09:09:25+0900",
      "summary": "ocr-service (FastAPI, stateless) — 3 endpoints. app/schema.py: ExtractedFields (10 Optional fields: owner_name/owner_ssn/owner_address/vehicle_reg_no/vehicle_vin/vehicle_model/vehicle_year str|None, vehicle_mileage/vehicle_weight/vehicle_total_weight int|None), ExtractResponse{fields, raw, status:Literal['ok','partial','failed'], warnings:list[str], error_code:str|None}, FillPdfRequest (same logical fields all str|None + current_date). app/codex_client.py: run_codex_ocr(image_path) runs `codex exec --skip-git-repo-check -s read-only --color never -i <img> -o <tmpfile> <PROMPT>` via subprocess.run(timeout=90, cwd=tmpdir), reads -o last-message file (falls back to stdout); typed exceptions CodexUnavailable/CodexAuth/CodexRateLimit/CodexTimeout/CodexBadOutput distinguished by exit code + auth/rate substring patterns in stdout+stderr; codex_health()->'ok'|'missing'|'unauthenticated'|'unknown' via `codex --version` + ~/.codex/auth.json existence (no OCR call). app/extract.py: extract_from_upload(data,filename)->ExtractResponse, NEVER raises, NEVER fakes. Flow: magic-byte/Pillow open (PDF -> pypdfium2 render page 0 only @scale 2.0) -> downscale long side to 2000px -> save temp JPEG q90 -> run_codex_ocr -> defensive JSON parse (first '{'..last '}', then ```json fence) -> normalize (vin re.sub [^A-Z0-9] uppercase + 17-char warning, mileage/weight/total_weight strip non-digits->int else None+warning, address \\s+->space, reg_no \\s+->space, year extract (19|20)\\d{2} else raw); status ok if all 10 set, partial if some empty/missing, failed if image bad / codex error / non-JSON. error_code map: bad image->OCR_BAD_IMAGE, CodexUnavailable->OCR_UNAVAILABLE, CodexAuth->OCR_AUTH, CodexRateLimit->OCR_RATE_LIMIT, CodexTimeout->OCR_TIMEOUT, CodexBadOutput/non-JSON->OCR_BAD_OUTPUT. Module-level threading.Semaphore(2) for concurrency. app/fill_pdf.py: PDF_FIELD_NAMES=['owner_name','owner_ssn','owner_address','vehicle_reg_no','vehicle_vin_1','vehicle_vin_2','vehicle_model','vehicle_year ' (TRAILING SPACE),'vehicle_mileage','vehicle_weight_1','vehicle_weight_2','current_date']; asserted == template get_fields() on first use; template path = TEMPLATE_PATH env or assets/malso_application_template.pdf (PdfTemplateMissing if absent). fill(req)->(bytes, missing_important:list[str]) where important=['owner_name','vehicle_reg_no','vehicle_vin_1']; logical->PDF mapping: vehicle_vin -> vin_1 & vin_2 (same), vehicle_weight->weight_1, vehicle_total_weight->weight_2 (falls back to weight_1), vehicle_year->'vehicle_year ', current_date defaults to f'{y}년 {m}월 {d}일'; None/empty -> ''; pypdf PdfWriter clone -> update_page_form_field_values per page (auto_regenerate=False) -> set_need_appearances_writer(True) -> bytes. NO reportlab overlay fallback needed (pypdf path passes tests). app/main.py: GET /health -> {status:'ok', codex:codex_health()}; startup hook writes CODEX_AUTH_JSON env -> ~/.codex/auth.json (chmod 600) if set; POST /extract (UploadFile) -> 400 if missing/empty, 429 {error.code:OCR_BUSY} if semaphore full, else 200 ExtractResponse (runs extract in asyncio.to_thread); POST /fill-pdf (FillPdfRequest JSON) -> Response(application/pdf, header X-Missing-Fields) or 500 {error:{code:PDF_TEMPLATE_MISSING|PDF_FILL_FAILED}}. tests/test_ocr.py: 15 tests (fill all fields / field-names-match / defaults+missing / parse pure-json|codefence|prose+json|garbage / normalize + bad-number-warns / extract bad-image|empty / codex mocked partial + all-5-error-codes / health) — all pass. codex CLI present (0.128.0) AND authenticated: real OCR run against assets/samples/자동차등록증_레이.jpg returned status='partial' with correctly extracted owner/vin/model/year/mileage/total_weight (only vehicle_weight blank). `npm run build` clean (backend/frontend unaffected).",
      "completed_at": "2026-05-13T09:30:00+0900"
    },
    {
      "step": 3,
      "name": "backend-api",
      "status": "pending"
    },
    {
      "step": 4,
      "name": "frontend",
      "status": "pending"
    },
    {
      "step": 5,
      "name": "deploy",
      "status": "pending"
    },
    {
      "step": 6,
      "name": "polish-and-verify",
      "status": "pending"
    }
  ],
  "created_at": "2026-05-13T08:53:00+0900"
}