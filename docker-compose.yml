# TODO(step5): local dev stack — postgres, backend, ocr-service, frontend.
# Mount the host ~/.codex into the ocr-service container read-only (see docs/ADR.md ADR-008).
# Skeleton only — see docs/ARCHITECTURE.md §2.1 and the step5 plan.
